<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>
        {{ encore_entry_link_tags('app') }}
        {% block stylesheets %}
        {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-static-top">
        <div class="navbar-header">
            <a href="{{ url('reservation') }}" class="navbar-brand">Rezerwacje</a>
            <a href="{{ url('workplace') }}" class="navbar-brand">Miejsca pracy</a>
        </div>
    </nav>

    <section class="content">
            <div class="col-lg-12" id="content-main">
                {% block body %}{% endblock %}
            </div>
        </section>
        <script>
            var $root;
        </script>
        {{ encore_entry_script_tags('app') }}
        {% block javascripts %}
        {% endblock %}
    </body>
</html>
